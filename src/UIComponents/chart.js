import Chart from "chart.js";const ctx = document.getElementById("myChart").getContext("2d");//ctx.defaults.scale.ticks.beginAtZero=true;function chartData(positive,negative,neutral){    const tweetData= [positive.length-1, negative.length-1,neutral.length-1];    let data = {        datasets: [{            data: tweetData,            backgroundColor: ["red","blue","green"]        }],        labels: [            'positive tweets ',            'negative tweets',            'neutral tweets'        ]    };    let myPiechart= new Chart(ctx,{        type:"doughnut",        data:data,        options:{        }    })}export default chartData;